import React, {Component} from 'react'
import {connect} from 'react-redux';
import {}

const mapStateToProps = state => {
	const {
		_id,

	} = state.auth
	return {

	}
}

export default connect (mapStateToProps, {}) (class ChatBox extends Component {
	
	constructor() {
		super()

	}
	render() {
		return (
			<div></div>
		)
	}
})