const {authenticate} = require('./authenticate');
const {blog} = require('./blog');
const {client} = require('./client');
const {chat} = require('./chat');
const {tutor} = require('./tutor');

module.exports = {authenticate, blog, client, chat, tutor};