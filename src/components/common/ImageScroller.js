import React { Component } from 'react';

class ImageScroller extends Component {
	render(props){
		const { width, height, images, scrollWith } = this.props;
		state = {location: 1};
		
		return(
			<View>
		)
	}
}

export default ImageScroller;
